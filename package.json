{
  "name": "to-do-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node index.js",
    "prebuild": "cd backend && npm install",
    "build": "cd frontend && npm run build"
  }
}